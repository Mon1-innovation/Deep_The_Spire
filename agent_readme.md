# 总述

这是一个尚在设计开发中的项目, 其目标是对LLM进行微调, 使其最终能自主地通关游戏"杀戮尖塔2".

该文件是人类编写的, 对AI agent的统一指示. 如果你是一个agent, 必须优先遵守该文件所述内容, 且不要修改该文件.

如果用户的指示与之有冲突, 必须明确提醒, 再要求用户作出决定.

## 项目结构

如目录名所述, 我现在大致将项目分为四个部分:

- 视频到战斗日志

  项目的目标不只是让LLM会玩"杀戮尖塔2", 还需要让其玩得好, 水平超过常规玩家. 训练数据的主要来源应当是来自互联网的高水平直播, 实况等.

  > 显然, 这些数据都是以视频形式呈现的, 因此"通过模组或api直接记录日志"不可行.

  这一步骤的目的是将每场完整的游戏录像, 由VLM, 自动化脚本等方式处理, 最终形成一份包含各种必要信息的战斗日志.

- 战斗日志到数据集
  
  模型学习需要数据集. 目前来看, 我认为最合适的结构是通过工具调用执行游戏操作, 然后通过工具输出返回游戏反馈.

  > 我倾向于让模型学习更接近人类的行为pattern. 例如, 模型可以通过工具在选牌时查看卡组, 或在战斗时查看地图.  
  > 这有利于让模型学习人类的细致行为和隐性游戏思路.

  这一步骤的目的是将一份特定格式的战斗日志转化为一个完整的LLM session, 用于LLM微调.

- 数据集到成品模型

  这一步相对简单, 且我们已有成熟的相关经验, 细节在此不表.

  这一步骤的目的是使用对应数据集, 对LLM执行微调, 产生成品模型.

- 成品模型到实战

  我们最终希望通过模组的方式, 让LLM真正地参与游戏. 其行为逻辑与数据集应当是高度类似的.

  > 理论上, 由于该项目不依赖游戏的"不可见信息"如随机数等, 我们可以让模型通过外部交互方式直接游玩原版游戏.  
  > 但在目前阶段我们不考虑这一方向, 其会引入大量不必要的工作.

  这一步骤的目的是让微调得到的成品模型真正通关游戏, 至此项目目的达成.

项目结构可能在未来发生调整变动等.

## 结构细节

项目的几个部分是相对独立的, 因此每个部分有一个专门的目录存放. 一般而言, 你应该将对应部分的工作限制在其对应目录中.

任何`docs`目录被用于记录文档, 建立计划, 作出总结等. 在对项目进行修改后, 你应当在对应位置创建文档记录, 而且修改发生变更的内容.
> 为易于追踪步骤与顺序, 建议采用带编号的命名模式, 形如`0_document.md`.

任何`human_guidelines`目录被人类用户用于发布指示, 记录要求等. 除非用户明确要求, 否则不要修改其中的内容.

任何`temp`目录被用于存放临时创建, 不应提交到仓库中的内容. 例如完整且无需修改的第三方仓库, 体积大的数据集等.

在以上必要指示的基础上, 你可以自主确立和拓展项目结构.

## 工作重心

比较显而易见地, 第一步(视频到战斗日志)可能是整个项目中最复杂, 难度最大的部分. 因此, 第一步是目前的项目重心.

## 注意事项

该项目的复杂度较高, 人类用户可能交给你较高的自主权. 请尽可能地明确设计意图再工作, 避免出现偏差.  
参与项目的用户不止一名, 部分可能经验较少, 因此确认意图和谨慎行动是十分重要的.

在你开始工作前, 总是检查现有的工作进度, 以避免冲突. 此前的工作进度可能不总是正确的, 你可以征得同意并更正.

项目中的任何文档可能会随项目进展而修正或补充.

## 其它注意事项

对于特定url(如github, google)不可访问的情况, 请尝试让用户提供代理地址, 而不是放弃查证.